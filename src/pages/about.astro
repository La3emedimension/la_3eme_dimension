---
import BaseLayout from '../layouts/BaseLayout.astro';

import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
---

<BaseLayout title="A propos | La 3ème dimension" description="About Jeanine White Lorem Ipsum">
	<div class="stack gap-20">
		<main class="wrapper about">
			<Hero
			align="center"
				title="A propos"
				tagline="Bonjour, je suis Raphaël et j'ai eu l'idée de crée La 3eme dimension un concept d'approche différent de design, dans l'optique d'apporter un oeil et une expertise adapté aux besoins et à la demande de chacun, j'ai concu plusieurs objets (télescope, lunette astronomique etc) qui m'ont demandé d'être extremement rigoureux. J'ai pour objectif de mettre se savoir faire au profit de vos besoins. "
			
				>
			</Hero>

			<img
					width="1553"
					height="873"
					src="/assets/at-work.jpg"
					alt="Portrait"
					/>

		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.about {
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
		
		
	}

	img {

		margin-top: 1.5rem;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);		
	}

	section {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		color: var(--gray-200);
		
	}

	.section-title {
		grid-column-start: 1;
		font-size: var(--text-xl);
		color: var(--gray-0);
	}

	.content {
		grid-column: 2 / 4;
	}

	.content :global(a) {
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.content :global(a:hover),
	.content :global(a:focus) {
		text-decoration-color: currentColor;
	}
	.hero {
		text-align :"center";
	 }

	@media (min-width: 50em) {
		.about {
			display: grid;
			grid-template-columns: 1fr 60% 1fr;
			text-align: left;
		}

		.about > :global(:first-child) {
			grid-column-start: 2;
		}

		section {
			display: contents;
			font-size: var(--text-lg);
		}
	}
	
</style>
