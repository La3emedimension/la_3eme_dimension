---
import BaseLayout from '../layouts/BaseLayout.astro';
import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
import { localePath, type Locale } from '../i18n';

const locale: Locale = 'fr';
---

<BaseLayout locale={locale} title="A propos | La 3ème dimension" description="About La 3ème dimension">
	<div class="stack gap-20">
		<main class="about">
			<Hero title="L'origine du projet">
				<p>La 3ème Dimension est née d'un constat : les télescopes portables du commerce font souvent des compromis frustrants entre encombrement et qualité optique. J'ai voulu concevoir un instrument qui tienne dans un sac à dos, se monte en quelques minutes, et offre de vraies performances.</p>

				<p>C'est ainsi qu'est né le Smallest, un Newton 150/750 entièrement imprimable en 3D. Le projet cumule plus de 23 000 vues sur Printables et fédère une communauté active de constructeurs.</p>

				<p>Pour une qualité sans compromis, je propose une version assemblée et optimisée : tubes carbone démontables pour un rangement encore plus compact, lestage amovible pour un équilibrage parfait selon vos oculaires, le tout testé et garanti 2 ans.</p>

				<p>Mon parcours mêle travail du bois, dessin industriel et, depuis 2016, communication et design. Cette double compétence technique et visuelle guide ma façon de concevoir : des objets fonctionnels, robustes, et soignés.</p>

				<p>Au quotidien, j'accompagne restaurateurs, artisans et particuliers sur leurs besoins en impression 3D : pièces de rechange, prototypes, solutions sur mesure.</p>

				<p style="text-align: right; font-style: italic; margin-top: 2rem;">— Raphaël</p>

			</Hero>	
			<img src="/assets/apropos.jpg">
		
		</main>

		<ContactCTA locale={locale} />
	</div>
</BaseLayout>

<style>
	p{
		text-align: left;
		font-size: 70%;
		margin-right: 15px;
		margin-left: 15px;
	}
	
	.about {
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
		align-items: center;
		margin-right: 15px;
		margin-left: 15px;
	}

	img {
		max-width:30vh;
		width: 40vh;
		margin-right: 5%;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);		
	}

	section {
		display: flex;
		flex-direction: row;
		gap: 0.5rem;
		color: var(--gray-200);		
	}

	.section-title {
		font-size: 30%;
		grid-column-start: 1;
		font-size: var(--text-xl);
		color: var(--gray-0);
	}

	.content {
		grid-column: 2 / 4;
	}

	.content :global(a) {
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.content :global(a:hover),
	.content :global(a:focus) {
		text-decoration-color: currentColor;
	}

	@media (min-width: 50em) {
		
		.about {
			display: flex;
			flex-direction: row;
			text-align: center;
			margin-left: 5%;
		}

		.about > :global(:first-child) {
			grid-column: 1/3;
		}

		section {
			display: contents;
			font-size: var(--text-lg);
		}

	}
	
</style>
